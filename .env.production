# Loaded by Vite only in production mode (`pnpm build`). Overrides nothing else in .env —
# Vite merges .env + .env.[mode], mode-specific values win for matching keys. See
# README.md's Development section.

# Deployed backend-proxy Cloudflare Worker.
VITE_AGENT_PROXY_URL=https://arfhewallet-agent-proxy.arfhewallet.workers.dev
