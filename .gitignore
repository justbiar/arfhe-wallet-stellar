# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
vite.config.js

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Bone-headed .env xD
.env
# Backups and per-environment copies carry the same secrets as .env itself.
.env.*
!.env.example
bun.lockb

*.xpi

# Hardhat build output. Regenerated by `npm run compile`, and every recompile
# rewrites hundreds of files — tracking them buries real changes in review noise.
deploy/artifacts/
deploy/cache/
