{
  "name": "arfhe-deploy",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:sepolia": "npx hardhat run scripts/deploy-shielded.js --network sepolia",
    "deploy:arb": "npx hardhat run scripts/deploy-shielded.js --network arbitrumSepolia",
    "deploy:base": "npx hardhat run scripts/deploy-shielded.js --network baseSepolia"
  },
  "devDependencies": {
    "@fhenixprotocol/cofhe-contracts": "^0.1.3",
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^16.4.7",
    "fhenix-confidential-contracts": "^0.3.1",
    "hardhat": "^2.29.0"
  }
}
